// 승부예측 주간 이벤트(경품 추첨) 활성 여부.
// 전반기 종료로 잠시 중단 → 후반기에 true 로 되돌려 재개한다.
// 이 값을 끄면 이벤트 유도 UI(EVENT 뱃지 · 나 vs AI 대결 섹션 · 예측 후 로그인 유도 모달)가 숨겨진다.
// 이미 발표된 "당첨자 발표"(배너·공지·당첨 모달·경품 문의)는 이 플래그와 무관하게 유지된다.
export const WEEKLY_EVENT_ACTIVE = false;
